[package]
name = "sima-api"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
sima-contracts = { path = "../sima-contracts" }
sima-core = { path = "../sima-core" }
sima-model = { path = "../sima-model" }
# For `serve` alone: the entry point that hosts a program's plugs over the two
# conversations a run has with it.
sima-transport = { path = "../sima-transport" }

[lints]
workspace = true
